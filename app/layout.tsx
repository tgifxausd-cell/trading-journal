import './globals.css'
export const metadata={title:'TradeLog — Trading Journal',description:'Private trade journal for Indian and global markets'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
